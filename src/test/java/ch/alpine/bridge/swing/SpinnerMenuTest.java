// code by jph
package ch.alpine.bridge.swing;

import org.junit.jupiter.api.Test;

class SpinnerMenuTest {
  @Test
  public void testSimple() {
    // SpinnerLabel<Pivots> spinnerLabel = SpinnerLabel.of(Pivots.class);
    // new SpinnerMenu<>(Arrays.asList(),null,null, false).design(new JPopupMenu());
    // new SpinnerMenu<>(Arrays.asList(),null,null, true).design(new JPopupMenu());
  }
}
