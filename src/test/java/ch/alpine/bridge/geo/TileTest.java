// code by jph
package ch.alpine.bridge.geo;

import java.util.HexFormat;

import org.junit.jupiter.api.Test;

class TileTest {
  @Test
  void test() {
    IO.println(HexFormat.of().toHexDigits(63));
  }
}
