// code by jph
package ch.alpine.java.ref.ann;

public enum FieldListType {
  /** appears as single line text field with selected value as text
   * a button to display options in a drop down menu */
  TEXT_FIELD,
  /** appears as list with each options as list item
   * in which selected item is highlighted */
  LIST,
  /** appears as list of radio buttons with
   * selected item with highlighted knob */
  RADIO, //
  ;
}
